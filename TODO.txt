User:
Client:
-cart[item]
-purchaseHistory[order]

update user (Go over again when we have the relations ready)

Store:
getList(UserId), getList(), getById

Item:
getList(storeId), getList(category) WITH PAGINATION, delete // TODO merge into single filter?

Order:
getList(storeId) ?

// TEST
FrontPageFeed
Item get by store